receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  logging:
    logLevel: info
  prometheus:
    endpoint: "0.0.0.0:8888"

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logging]
    metrics:
      receivers: [otlp]
      exporters: [prometheus]
